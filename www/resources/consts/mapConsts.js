const globalHitTolerance = 20;

const selectedColor = "rgb(255, 153, 0)";

const defaultNumberNodesOnMap = 10000;

const GPS_LAYER_ID = 'gpsLayer';
const GEO_MARKER_NAME = 'geoMarker';
const GPS_ACCURACY_NAME = 'gpsAccuracyName';

const NAVIGATION_MODE = {
	DISABLED: 'Disabled',
	TURN_NAVIGATION_ARROW: 'Turn navigation arrow',
	HOLD_CENTER_MAP: 'Hold center map'
}